function drawCompulsionChart(stageWidth, stageHeight, renderer) {
    // Implementation for drawing the compulsion chart
    console.log('Drawing compulsion chart');
    // Clear existing content
    renderer.empty();
    // Draw compulsion chart logic here
}